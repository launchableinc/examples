name: "docker launchable actions"
description: "sample launchable actions"
author: "launchableinc"
inputs:
  test:
    required: true
    description: "something"
    default: "aaabbb"
runs:
  using: "docker"
  image: "Dockerfile"
  entrypoint: "launchable verify"
branding:
  icon: "message-square"
  color: "orange"
